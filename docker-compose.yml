networks:
  web:          # réseau interne de l’app
    driver: bridge
  proxy:        # réseau partagé avec Traefik (externe)
    external: true

volumes:
  postgres_data:
  static_volume:
  media_volume:

services:
  # --- Base de données Postgres + PostGIS ---
  tratradb:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 20
    networks: [web]
    restart: unless-stopped

  # --- Redis (broker Celery + cache + Channels) ---
  redis:
    image: redis:7
    command: ["redis-server","--save","","--appendonly","no"]
    healthcheck:
      test: ["CMD", "redis-cli", "PING"]
      interval: 10s
      timeout: 5s
      retries: 20
    networks: [web]
    restart: unless-stopped

  # --- API Django/ASGI (Daphne) ---
  tratraweb:
    build:
      context: .
    dockerfile: Dockerfile
    env_file: .env
    environment:
      # sécurise les valeurs critiques au cas où .env ne serait pas lu
      DJANGO_SETTINGS_MODULE: "tratra.settings.prod"
      DB_HOST: "tratradb"          # <— IMPORTANT : correspond au service DB
      DB_PORT: "5432"
      CELERY_BROKER_URL: "redis://redis:6379/0"
      CELERY_RESULT_BACKEND: "redis://redis:6379/0"
    command: ["/app/docker/entrypoint.sh", "daphne", "-b", "0.0.0.0", "-p", "8000", "tratra.asgi:application"]
    depends_on:
      tratradb:
        condition: service_healthy
      redis:
        condition: service_healthy
    labels:
      - traefik.enable=true

      # FQDN de l’API exposée par Traefik (mets la variable dans .env)
      - traefik.http.routers.tratraweb.rule=Host(`${TRATRA_HOST}`)
      - traefik.http.routers.tratraweb.entrypoints=websecure
      - traefik.http.routers.tratraweb.tls.certresolver=lets

      # Port interne du conteneur à publier via Traefik
      - traefik.http.services.tratraweb.loadbalancer.server.port=8000

      # Middlewares (sécurité / compression / rate-limit)
      - traefik.http.middlewares.tratraweb-sec-headers.headers.stsSeconds=31536000
      - traefik.http.middlewares.tratraweb-sec-headers.headers.stsIncludeSubdomains=true
      - traefik.http.middlewares.tratraweb-sec-headers.headers.stsPreload=true
      - traefik.http.middlewares.tratraweb-sec-headers.headers.contentTypeNosniff=true
      - traefik.http.middlewares.tratraweb-sec-headers.headers.frameDeny=true
      - traefik.http.middlewares.tratraweb-sec-headers.headers.referrerPolicy=no-referrer
      - traefik.http.middlewares.tratraweb-compress.compress=true
      - traefik.http.middlewares.tratraweb-rl.ratelimit.average=150
      - traefik.http.middlewares.tratraweb-rl.ratelimit.burst=250
      - traefik.http.routers.tratraweb.middlewares=tratraweb-sec-headers@docker,tratraweb-compress@docker,tratraweb-rl@docker

    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    networks:
      - web
      - proxy
    restart: unless-stopped

  # --- Celery Worker ---
  worker:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: "tratra.settings.prod"
      DB_HOST: "tratradb"
      DB_PORT: "5432"
      CELERY_BROKER_URL: "redis://redis:6379/0"
      CELERY_RESULT_BACKEND: "redis://redis:6379/0"
    command: celery -A tratra worker -l info --prefetch-multiplier=1 --max-tasks-per-child=200 --queues=default,notifications
    depends_on:
      tratradb:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks: [web]
    restart: unless-stopped

  # --- Celery Beat (planificateur) ---
  beat:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: "tratra.settings.prod"
      DB_HOST: "tratradb"
      DB_PORT: "5432"
      CELERY_BROKER_URL: "redis://redis:6379/0"
      CELERY_RESULT_BACKEND: "redis://redis:6379/0"
    command: celery -A tratra beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    depends_on:
      tratradb:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks: [web]
    restart: unless-stopped
  # --- (Optionnel) Flower monitoring ---
  # flower:
  #   image: mher/flower
  #   command: flower --broker=${REDIS_URL}
  #   environment:
  #     FLOWER_BASIC_AUTH: ${FLOWER_AUTH}
  #   labels:
  #     - traefik.enable=true
  #     - traefik.http.routers.flower.rule=Host(`${FLOWER_HOST}`)
  #     - traefik.http.routers.flower.entrypoints=websecure
  #     - traefik.http.routers.flower.tls.certresolver=lets
  #     - traefik.http.services.flower.loadbalancer.server.port=5555
  #   networks: [web]
  #   restart: unless-stopped

#services:
#  tratraDB:
#    image: postgres:15
#    environment:
#      POSTGRES_USER: postgres
#      POSTGRES_PASSWORD: postgres
#      POSTGRES_DB: tratra
#    volumes:
#      - postgres_data:/var/lib/postgresql/data
#    ports:
#      - "5432:5432"
#
#  redis:
#    image: redis:7
#    ports:
#      - "6379:6379"
#
#  tratraweb:
#    build: .
#    command: >
#      sh -c "python manage.py migrate &&
#             python manage.py runserver 0.0.0.0:8000"
#    volumes:
#      - .:/app
#    ports:
#      - "8000:8000"
#    depends_on:
#      - tratraDB
#      - redis
#
#  celery:
#    build: .
#    command: celery -A tratra worker -l info
#    volumes:
#      - .:/app
#    depends_on:
#      - db
#      - redis
#
#  celery-beat:
#    build: .
#    command: celery -A tratra beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
#    volumes:
#      - .:/app
#    depends_on:
#      - tratraDB
#      - redis
#
#  flower:
#    image: mher/flower
#    command: flower --broker=redis://redis:6379/0
#    ports:
#      - "5555:5555"
#    depends_on:
#      - redis
#
#volumes:
#  postgres_data: